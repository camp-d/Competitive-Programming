#include<bits/stdc++.h>

typedef long long ll;


void solve(){

    int size;
    std::cin >> size;

    std::vector<int> nums(size, 0);

    for(auto &x : nums){
        std::cin >> x;
    }



}

int main()
{
std::ios_base::sync_with_stdio(false); 
std::cin.tie(0); 
std::cout.tie(0);

int n;

std::cin >> n;

while(n--)
    solve();


return 0;
}
